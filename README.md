# graphs

For the second project we had to modify the toString method in Node. Back then the to String method return a String representing the number of the node. Now it return it's name if it have one, and it's number if not.


